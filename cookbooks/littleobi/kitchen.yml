---
driver:
  name: vagrant
  synced_folders:
   - ['.priv', '/tmp/priv']
  customize:
        memory: 2048
        cpus: 2
    

provisioner:
  name: chef_zero
  data_bags_path: "../../data_bags/"
  
  
platforms:
  - name: centos-8

suites:
  - name: default
    run_list:
      - recipe[littleobi::default]
    attributes:
